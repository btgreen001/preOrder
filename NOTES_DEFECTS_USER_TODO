Using the Coding agent and using NOTES_DEFECTS_USER_TODO for quick work referencing, ALWAYS follow instructions at .github/instructions/copilot-instructions.md, 
and always keeping MASTER_TODO_LIST.md updated on progress, 

/shop is BROKE....the org is not honoring who I am after I register and login.
Need a forget user/password; and need page to be able to perform change password

As part of company registration we need to ensure license is setup.
AS Part of Initial prod deployment Fix this to 30 -  "BindRateLimitCount": 300,
AS Part of Initial prod deployment This to 120 - "BindRateLimitWindowSeconds": 12,
AS Part of Initial prod deployment These to 10 and 60  
  <!-- ,
  "RateLimiting": {
    "RefreshToken": {
      "RequestLimit": 100,
      "TimeWindowSeconds": 6
    }
  } -->
---



## 2026-04-25 High Priority Work Completed

✅ **Code Verification** (All Critical Paths Validated):
- Pickup slot window validation (backend + frontend) — Implemented with EnsureSlotWithinEventPickupWindow() and isSlotWithinEventPickupWindow()
- OrderItem.MenuItemId refactor — Correctly replaces SellableProductId, preserves order intent
- Admin soft-delete with dependency checks — Prevents deactivation of entities with active references
- Public order flow with capacity guards — hasPickupCapacity getter blocks progression when no slots available
- CSV export wired in admin orders view — exportCsv() connected to endpoint

✅ **Regression Test Plan Created** (REGRESSION_TEST_PLAN.md):
- 20 test scenarios covering public flow, admin CRUD, refactor validation
- All critical flows documented with expected outcomes
- Ready for manual/automated testing against live environment

✅ **Build Status**:
- No TypeScript errors (get_errors returned clean)
- No C# compilation errors
- Both `/api/` and `/web/` ready for testing

## 2026-04-25 MVP Gaps Clarified (Action Required)

- Onboarding is **not finalized** and remains an MVP blocker.
- Required completion path:
	- Company registration -> valid admin landing route
	- Tenant-admin invite code management (create/list/deactivate)
	- Staff registration using invite codes with clear invalid/expired handling

- Public storefront shell cleanup is **required**:
	- `/shop` (and `/store` if used) must not show admin/company sidebar links
	- Hide role badge, change user, and logout controls on public storefront views
	- Keep those controls available on admin routes only

- Product scope clarification:
	- Full sellable-product CRUD is **not** an MVP blocker
	- Current Unlinked placeholder strategy is acceptable for MVP

- Definition of done update:
	- MVP is complete only after onboarding finalization + storefront shell cleanup + regression pass

## 2026-04-26 Next Steps (Execution Priority)

1. **Public storefront shell cleanup (first)**
	- Update Angular root shell so `/shop` does not render admin sidebar links.
	- Hide role badge, change user, and logout controls on public storefront routes.
	- Keep those controls on admin routes only.

2. **Onboarding flow completion (second)**
	- Confirm company registration lands on an existing route.
	- Finalize tenant-admin invite code management path (API + UI).
	- Validate staff sign-up via invite code with clear invalid/expired feedback.

3. **Regression and status update (third)**
	- Execute focused tests for storefront shell behavior and onboarding flow.
	- Immediately update `MASTER_TODO_LIST.md` and this file with test results and blockers.

## 2026-04-26 Progress Update

- ✅ **Public storefront shell cleanup completed**
	- Angular root shell is now route-aware for storefront routes.
	- On `/BakeAhead` storefront route, admin sidebar and top-right auth controls are hidden.
	- On admin routes, full admin shell remains visible.
	- Added admin-facing `Preview Store` link in the top navigation that opens `/BakeAhead` in a new tab, allowing admins to preview storefront without leaving admin workflow.
	- Added route definition for `/BakeAhead` and legacy compatibility redirect from `/shop`.
	- Files updated: `web/src/app/app.ts`, `web/src/app/app.html`, `web/src/app/app.routes.ts`.

- ✅ **Onboarding route-flow stabilization completed (partial Step 2)**
	- Restored missing routes used by registration/login utility flows:
		- `/dashboard`
		- `/terminal-selection`
		- `/pin-signin`
	- This prevents company-register/login/terminal/PIN flows from falling into wildcard redirect due to missing routes.
	- File updated: `web/src/app/app.routes.ts`.
	- Remaining onboarding blocker: tenant-admin invite code management (API + UI).

- ✅ **Rate-limit config issue resolved (backend)**
	- Symptom: increasing rate-limit values in appsettings had no effect for `/api/auth/refresh-token`.
	- Root cause: `RateLimitingMiddleware` used hard-coded constructor defaults (10 requests / 60s) instead of configuration.
	- Fix: middleware now reads:
		- `RateLimiting:RefreshToken:RequestLimit`
		- `RateLimiting:RefreshToken:TimeWindowSeconds`
	- Added keys in both:
		- `api/appsettings.json`
		- `api/appsettings.Development.json`
	- Note: terminal bind limits were already config-driven via `Terminal:BindRateLimitCount` and `Terminal:BindRateLimitWindowSeconds`.

- ✅ **Invite email send + resend implemented (SMTP)**
	- SMTP provider configured in both appsettings files:
		- Host: `smtp.gmail.com`
		- Port: `465`
		- SSL: enabled
		- Username: `boardbake@gmail.com`
		- Password: app password placeholder set in config for local setup.
	- Backend create-invite endpoint now sends email automatically when invitee email is provided.
	- Added resend endpoint:
		- `POST /api/organization/{orgId}/registration-codes/{codeId}/resend`
	- Resend guardrails:
		- reject used/expired/no-email invites
		- throttle to max 3 resends per code per hour
	- Frontend invites page now includes `Resend` button for eligible invites and handles success/error feedback.
	- Registration page now supports invite deep links with query-prefill (`?code=...&email=...`).


2026-04-23 preorder persistence cutover notes
- `MvpPreOrderService` now writes holiday preorders into `customer_order` and `order_item` instead of `pre_order` and `pre_order_line`.
- `menu_item.product_id` is now the required bridge for any menu item that should be orderable through the preorder flow.
- Existing environments may still need manual SQL to add any missing `customer_order` preorder linkage columns before runtime validation.
- Focused backend build verification was attempted but the integrated terminal returned alternate-buffer behavior instead of usable build output.
- Added a startup compatibility patch in `Program.cs` to create `menu_item.product_id` automatically when older databases do not have that column yet.
- Restart the API once so the startup patch runs before testing `/api/public/preorders/menu-items` again.
- Added runtime fallback in `MvpPreOrderService.CreatePreOrderAsync` to auto-resolve missing `menu_item.product_id` links by unique product-name match (active, for-sale products only).
- If a menu item still cannot be matched uniquely, API now fails with a consolidated list of unmatched menu item names.
- Implemented preorder CSV export for admin operations at `GET /api/mvp/preorders/export.csv`.
- CSV supports optional query filters: `holidayEventExternalId` and `pickupDateUtc`.
- Export output includes header + line-level rows to support pickup-day packing and order review workflows.
- Added Angular preorder-admin feature service and routes:
	- `admin/orders` wired to live preorder list (`GET /api/mvp/preorders`) and CSV download (`GET /api/mvp/preorders/export.csv`).
	- `admin/events`, `admin/menu`, `admin/slots` scaffolded as dedicated preorder pages for next CRUD wiring slice.
- Sidebar nav now exposes Events/Menu/Pickup Slots/Orders under admin for preorder work.
- Fixed API runtime crash in `GET /api/mvp/preorders` for mixed legacy data where `order_item.product_id` is stored as `varchar` values: service now catches `InvalidCastException` and falls back to header-only order reads.
- CSV export endpoint now uses the same fallback path under the same legacy condition.
- Remaining cleanup task: migrate/normalize legacy `order_item.product_id` values to numeric FK-compatible form.
- Added additional guard for Postgres SQLSTATE `42883` (`operator does not exist: character varying = bigint`) in menu lookup queries during preorder mapping.
- Service now returns empty lookup dictionaries for that legacy mismatch instead of crashing request execution.
- Extended the same SQLSTATE `42883` handling to `GetPreOrders` and CSV export include-query path (`Include(OrderItems)`) so legacy FK type drift there no longer 500s requests.
- Added final guard in `GetPreOrdersAsync` so if legacy mismatch still breaks the fallback query path, API returns a safe degraded result instead of throwing.
- Added additional `InvalidCastException` fallback in `BuildMenuItemLookupAsync` and `BuildMenuNameLookupAsync` to prevent materialization crashes while reading legacy `menu_item.product_id` values.
- Added nested fallback guards in `GetPreOrdersAsync` and `ExportPreOrdersCsvAsync` so a second cast/type-mismatch failure in the header-only fallback query path degrades safely rather than throwing 500.
- Root cause confirmed in live schema: `customer_order.preorder_event_id` and `customer_order.pickup_slot_id` were `varchar`; after converting both to `bigint`, preorder admin UI loads successfully.
- Post-fix validation: preorder CSV export also succeeds after the same `customer_order` type normalization.
- Added idempotent EF migration `20260423194500_NormalizeCustomerOrderPreorderForeignKeyTypes` to convert those two `customer_order` columns automatically in other environments, while failing clearly if non-numeric legacy values remain.
- **DATETIME POLICY FINAL**: Business times (event opens/closes/pickup windows, slot times) are wall-clock values stored and sent as-is without browser UTC conversion; audit timestamps (created/updated/cancelled) use `DateTime.UtcNow`. No remaining `toISOString()` calls on business datetimes in preorder flows. Policy documented in README.md and inline code comments (MvpPreOrderService, preorder-admin.service.ts) for future developers.
- **DESERIALIZATION FIX**: Added custom `WallClockDateTimeConverter` to parse frontend wall-clock datetime strings (YYYY-MM-DDTHH:mm from datetime-local inputs) into DateTime objects without timezone math. Applied via `[JsonConverter]` attributes on all business datetime request DTO fields.
- **DATA PROTECTION CONFIG**: Added ASP.NET Core DPAPI configuration in Program.cs to suppress warning about unencrypted keys at rest (safe for local dev; production should use Azure Key Vault or similar).
- **ERROR VISIBILITY FIX**: When users submit invalid data (e.g., close time before open time), they now see detailed error messages instead of generic "Could not save pre-order event."
  - Backend: `GlobalExceptionHandlerMiddleware` wraps all exceptions in `ApiErrorResponse` with message and field-level validation errors.
  - Frontend: `extractErrorMessage()` utility extracts and formats validation errors for display in error messages.
  - Applied to Events, Menu, and Slots admin forms.
- **EMPTY DATETIME FIX**: Fixed "The input does not contain any JSON tokens" 400 error by:
  - Strengthened frontend validation to reject empty datetime strings with specific field-level error messages (e.g., "Opens date/time is required.").
  - Updated `WallClockDateTimeConverter` to explicitly handle and reject empty strings with clear error messages.
  - Made converter more robust with explicit format list (YYYY-MM-DDTHH:mm, YYYY-MM-DD, ISO 8601) and fallback flexible parsing.
- **POSTGRESQL DATETIME KIND FIX**: Fixed "Cannot write DateTime with Kind=Local to PostgreSQL type 'timestamp with time zone'" error by changing converter to parse with `DateTimeStyles.AssumeUniversal`. This creates `DateTime(Kind=Utc)` compatible with PostgreSQL's `timestamp with time zone` columns while preserving wall-clock semantics on round-trip (no timezone conversion).
- **EVENT/slot EDIT MAPPING FIX**: Resolved issue where only `name` populated on Edit in admin pages. Root cause was casing mismatch: API returns camelCase (`opensAt`, `closesAt`, `pickupStartDt`, `pickupEndDt`, `slotStartAt`, `slotEndAt`) but frontend expected PascalCase. Updated preorder-admin service interfaces, events component/template, and slots component/template to camelCase keys.
- **WALL-CLOCK ROUND-TRIP FIX**: Updated `WallClockDateTimeConverter` to stop UTC standardization for business fields (`opensAt`, `closesAt`, `pickupStartDt`, `pickupEndDt`, `slotStartAt`, `slotEndAt`). It now parses as `DateTimeKind.Unspecified` and serializes without timezone suffix so pickup start/end save exactly as entered.
- **REQUEST BODY BINDING FIX**: Resolved recurring 400 (`The input does not contain any JSON tokens ... request field is required`) on `/api/mvp/menu-items` and related endpoints by restoring explicit `[FromBody]` attributes on all MVP JSON body actions in `MvpPreOrdersController`.
- **JSON CYCLE FIX**: Resolved `System.Text.Json.JsonException: A possible object cycle was detected` on preorder event responses by mapping `HolidayEvent` entities to flat response objects (no navigation collections) in `MvpPreOrdersController` before returning JSON.
- **JSON CYCLE HARDENING**: Extended flat response mapping to `menu-items` and `pickup-slots` endpoints as well, so these routes no longer serialize EF navigation graphs that can recurse.
- **LAYOUT/SCROLLBAR FIX**: Removed default browser page margin/padding on `html, body` in `web/src/styles.scss` to eliminate small top gap and phantom page overflow causing scrollbar to always show.
- **PREORDER PAYLOAD ENRICHMENT**: `GetPreOrdersAsync` now returns associated pickup slot data for each preorder by including `Order.PickupSlot` in query/fallback paths and mapping a safe slot snapshot (`pickupSlot`) into response objects.
- **PREORDER SUBMIT NULLABLE CRASH**: Restored the `SellableProductId` validation in `CreatePreOrderAsync` so menu items without linked sellable products return a clear validation error instead of crashing on `menuItem.SellableProductId.Value` with `Nullable object must have a value`.
- **UNLINKED PLACEHOLDER FLOW**: Menu item create/update now auto-assigns the active for-sale `Unlinked` sellable product when no explicit product is chosen, older null-linked menu items fall back to that placeholder during preorder creation, and the admin menu list shows a warning badge/banner so those placeholder-linked items are easy to review later.
- **EF QUERY TRANSLATION FIX**: `ResolveUnlinkedSellableProductIdAsync` no longer uses `NormalizeLookupName(...)` inside the EF query; it now compares with SQL-translatable `product.Name.Trim().ToUpper()` to avoid `could not be translated` runtime failures when creating/updating menu items.



**“Create a stack‑ranked list of silo products for a retail bakery order‑management system. Rank them based on a combined factor of build time, complexity, and revenue potential.

Assume:
– I am a solo developer with basic .NET and Angular skills and a Postgres backend.
– Build times should reflect realistic MVP timelines for someone at that skill level.
– Complexity should reflect technical difficulty, not feature count.
– Revenue potential should reflect what small retail bakeries typically pay for niche SaaS tools (usually $20–$150/mo for utilities, up to $200/mo for high‑value operational tools).
– Focus on practical, sellable micro‑SaaS modules like custom cake intake, holiday pre‑orders, simple online ordering, recipe costing, production lists, etc.

Provide the output as a table with:
– Rank
– Product / Silo
– Build Time (range)
– Complexity (Low/Medium/High)
– MRR Range (based on typical bakery willingness to pay)

Use realistic, grounded estimates similar to what you would give a beginner‑to‑intermediate developer building these tools alone.”**

BakeSweet℠  
Your bakery’s all‑in‑one management system.  
Powered by BakeBoard™

Modules included:
BakeAhead™ — Pre‑Order Manager
BakeCustom™ — Custom Cake Intake
BakeList™ — Production List Generator
BakeShop™ — Online Ordering
BakeCost™ — Recipe Costing

© 2026 [Your Company Name]. All rights reserved.
BakeSweet℠ and all related marks are trademarks of [Your Company Name].

Why These Rankings Make Sense
1. Holiday Pre‑Order Manager — #1 for a reason
Bakers hate managing Thanksgiving pie orders in spreadsheets.

MVP is simple:
Menu builder
Pickup slots
Order form
Export to CSV
Low complexity, high seasonal pain → easy to sell.

2. Custom Cake Intake
Every bakery has a chaotic cake‑order process.
MVP: form, image upload, quote calculator, status tracking.
Medium complexity but very high willingness to pay.

3. Production List Generator
Bakers want “tell me what to bake today.”
MVP: ingest orders → aggregate quantities → printable sheet.
Technically straightforward.

4. Simple Online Ordering
Higher complexity (cart, payments, availability rules).
But strong revenue potential because it replaces Shopify/Square.

5. Recipe Costing
Bakers love it, but it’s not mission‑critical.
Good add‑on revenue, not usually a standalone high‑MRR product.
6–10: Higher complexity, slower ROI
Wholesale portals, inventory, and scheduling are valuable but require more engineering (auth roles, multi‑tenant data, complex workflows).

If you want the fastest path to revenue
Start with:

1. Holiday Pre‑Orders
→ Launch in 2–3 weeks
→ Sell before major holidays
→ Easy onboarding

Then add:

2. Custom Cake Intake
→ High willingness to pay
→ Strong upsell path

Then:

3. Production Lists
→ Makes your system “operational,” not just intake

These three alone can realistically get you to $1k–$3k MRR as a solo dev.
If you want, I can also create:
A roadmap for building these in order
A pricing model
A feature‑scoped MVP plan for any one of them
A full product suite architecture showing how they connect
Which direction do you want to explore next?

Let's dig into Holiday Pre‑Order Manager (Thanksgiving pies, Christmas cookies, etc.)
Great — Holiday Pre‑Order Manager is one of the highest‑leverage bakery micro‑SaaS products you can build as a solo dev. Let’s break it down into something you can actually ship in 2–3 weeks and sell immediately.

Holiday Pre‑Order Manager — Deep Dive
1. What the MVP actually needs (and nothing more)
This is the smallest viable feature set that still solves a bakery’s seasonal pain.

Core MVP (Week 1–2 build)
Menu Builder

Item name, price, optional variants (size, flavor)

Inventory caps (e.g., “limit 200 pumpkin pies”)

Pickup Slot Manager

Define pickup days + time windows

Limit number of orders per slot

Customer Order Form

Mobile‑friendly form

Item selection + quantity

Pickup time selection

Customer info (name, phone, email)

Order Dashboard

List of orders

Filter by pickup day

Export to CSV / print

Basic Payment Options

MVP: “Pay at pickup” or simple Stripe checkout

Admin UX
Login (simple email/password)

Create/edit holiday event

Add menu items

View orders

What you don’t need for MVP
Full e‑commerce cart

Customer accounts

Multi‑location support

Complex tax/shipping logic

Automated reminders

Inventory forecasting

Those can come later as upsells.

2. Realistic Build Timeline (Solo Dev, .NET + Angular)
Week 1
Project setup (API + Angular)

Auth (JWT or cookie‑based)

Holiday event + menu CRUD

Order form UI

Week 2
Pickup slot logic

Order submission + validation

Admin dashboard

CSV export

Stripe integration (optional but recommended)

Week 3 (Polish + Launch)
Mobile optimization

Email confirmations

Error handling + logging

Deploy to Azure or Render

This is absolutely doable in 2–3 weeks if you keep scope tight.

3. Technical Complexity Breakdown
Low‑Complexity Areas
CRUD for menu items

Order form

CSV export

Admin dashboard

Medium‑Complexity Areas
Pickup slot capacity logic

Stripe integration

Multi‑tenant structure (if you want to sell to many bakeries)

High‑Complexity (avoid in MVP)
Real‑time inventory sync

Multi‑location

Automated production lists

Customer accounts

4. Revenue Potential & Pricing Strategy
Typical bakery willingness to pay
Seasonal tools: $40–$120/mo

If you bundle multiple holidays (Thanksgiving, Christmas, Easter, Mother’s Day), you can charge more.

Recommended pricing
$49/mo — Basic (pay at pickup only)

$79/mo — Stripe payments + pickup slot limits

$119/mo — Multi‑holiday + priority support

One‑time setup fee (optional but common)
$99–$199 for onboarding
Bakeries often prefer this because they want someone to “set it up for them.”

5. Why bakeries will buy this
Because their current workflow is usually:

Google Forms

Paper lists

Spreadsheets

Chaos during pickup day

Your tool solves:

Overbooking

Wrong pickup times

Manual tallying

Lost orders

Staff confusion

This is a high‑pain, high‑urgency problem.

6. Expansion Path (How this becomes a suite)
Once they trust your holiday system, you can upsell:

Custom Cake Intake

Production Lists

Simple Online Ordering

Recipe Costing

Holiday pre‑orders are the perfect wedge product.

7. If you want, I can generate next:
A full MVP architecture (API endpoints, DB schema, Angular components)

---

## Quick Work Notes (2026-04-23)

- Carve-out audit completed in `MASTER_TODO_LIST.md` with keep/adapt/remove across controllers, services, and models.
- Angular route map replaced with minimal preOrder skeleton in `web/src/app/app.routes.ts`.
- Backend MVP slice started:
	- New models: `HolidayEvent`, `MenuItem`, `PickupSlot`, `PreOrder`, `PreOrderLine`
	- New service contract: `IMvpPreOrderService`
	- New service implementation: `MvpPreOrderService`
	- New controller: `MvpPreOrdersController`
	- Wiring updates: `DbContext` + `Program.cs`
- Next immediate validation:
	- `dotnet build` in `api/`
	- `npm run build` in `web/`

## Progress Delta (latest)

- Public preorder flow now successfully creates orders and order lines in database.
- Public preorder response serialization cycle fixed by returning flat mapped payloads (no EF navigation graph in JSON response).
- Startup schema bootstrap made more tolerant during `holiday_*` -> `preorder_*` transition to reduce runtime breaks across mixed environments.

## Next Focus Queue

1. CSV export for preorders by event/date.
2. Replace remaining legacy admin UIs with preorder-specific admin screens.
3. Run regression checks for all anonymous public preorder endpoints.
4. Add narrow checks for admin preorder status transitions.

## Backend Slice Completed

- Added `PATCH /api/mvp/preorders/{preOrderExternalId}/status` for admin status updates.
- Allowed transitions:
	- `SUBMITTED` -> `CONFIRMED`
	- `SUBMITTED` -> `CANCELLED`
	- `CONFIRMED` -> `CANCELLED`
- Cancelling a preorder now decrements the pickup slot reserved count.
- Status changes are audit logged.
- Follow-up compile issue fixed: `MvpPreOrdersController` now imports `IOrganizationContextService` from `PreOrderApp.Services`.

A 3‑week development plan with daily tasks

A sales page for bakeries

A demo workflow you can show to prospects

A database schema optimized for multi‑tenant SaaS

Which one do you want to dive into next?