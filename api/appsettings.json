{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Host=192.168.50.78;Port=5432;Database=appdb;Username=appuser;Password=${DB_PASSWORD}"
  },
  "Paseto": {
    "SecretKey": "${PASETO_SECRET_KEY}",
    "AccessTokenExpirationMinutes": 15, 
    "__comment1": "//when the JWT access token expires - default 15 if not set",
    "RefreshTokenExpirationDays": 30,
    "__comment2": "//when the JWT refresh token expires if no activity - default 30 days if not set",
    "MaxSessionDurationDays": 90,
    "__comment6": "//when the JWT refresh token expires even with activity - default 90 days if not set",
    "__comment5 RefreshTokenAutoTrigger": "// is JWT - 2 minutes in auth.service.ts"
  },
  "Terminal": {
    "IdleTimeoutMinutes": 20,
    "__comment3": "//when the terminal idle timeout expires - default 30 minutes if not set; 1 to match 60s testing config",

    "CheckIdleIntervalSeconds": 90,
    "__comment4": "//interval to check for idle timeout in seconds typically this should be roughly 90",

    "BindCooldownSeconds": 5,
    "__comment5": "//cooldown period in seconds before the terminal can be bound again",
    "BindRateLimitCount": 30,
    "__comment6": "//maximum number of binds allowed within the rate limit window",
    "BindRateLimitWindowSeconds": 120,
    "__comment7": "//window in seconds for the rate limit of binds"

    
  }
  ,
  "RateLimiting": {
    "RefreshToken": {
      "RequestLimit": 10,
      "TimeWindowSeconds": 60
    }
  },
  "InviteEmails": {
    "Enabled": true,
    "FromEmail": "boardbake@gmail.com",
    "FromName": "BakeAhead",
    "RegisterBaseUrl": "https://192.168.50.50:4200/register",
    "Smtp": {
      "Host": "smtp.gmail.com",
      "Port": 587,
      "Username": "boardbake@gmail.com",
      "Password": "${SMTP_API_KEY}"
    }
  }
}
